# TODO.md

Granular immediate tasks:
- [ ] Add `isAdmin` and `isApproved` flags to Prisma User model.
- [ ] Add `isFlagged` flag to Prisma Post and Event models.
- [ ] Create `/admin` frontend dashboard with moderation tables.
- [ ] Install `ethers` library for mock blockchain integration.
- [ ] Add `/api/blockchain/mint` to simulate limited vinyl NFT creation.
