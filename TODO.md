# TODO.md

Granular immediate tasks:
- Finalize production integration of Stripe Connect (swap mock API with real Stripe instance).
- Enhance NextAuth Credentials flow to implement actual password hashing (bcrypt).
- Setup frontend OAuth login buttons for users/artists.